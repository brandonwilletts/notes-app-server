services:
  db:
    image: postgres
    container_name: postgres_production
    restart: always
    env_file: 
      - .env.production
    ports:
      - "5432:5432"
    volumes:
      - pgdata_production:/var/lib/postgresql/data 

volumes:
  pgdata_production: